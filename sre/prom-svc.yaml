apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: default
spec:
  selector:
    app: monitoring-stack
  type: LoadBalancer
  ports:
    - port: 8087
      targetPort: 9090
